import theme, { COLORS, SIZES, FONTS } from "./theme";
import images from "./images";
import dummyData from "./dummy";

export { 
    images,
    theme,
    COLORS, 
    SIZES, 
    FONTS,
    dummyData
};
